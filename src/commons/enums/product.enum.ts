enum ESale {
  sale,
  notSale,
}

enum EOutOfStock {
  unavailable,
  available,
}

enum EProductStatus {
  normal,
  bestSeller,
  comingSoon,
  hot,
  vip,
}

enum EProductClassType {
  first,
  second,
  firstClassName,
  secondClassName,
}

enum EProductMediaType {
  image,
  video,
}

export { ESale, EOutOfStock, EProductStatus, EProductClassType, EProductMediaType };
